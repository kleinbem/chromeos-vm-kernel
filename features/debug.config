# Kernel Debugging Tools
# ----------------------
# Enables symbols and logging for troubleshooting crashes.
# WARNING: This significantly increases the kernel file size (bzImage).

# Basic Kernel Debugging
CONFIG_DEBUG_KERNEL=y

# Enable Debug Info (Symbols)
# This allows tools like crash, gdb, or systemtap to read kernel states.
# It effectively reverses 'optimization.config'.
CONFIG_DEBUG_INFO=y
CONFIG_DEBUG_INFO_DWARF_TOOLCHAIN_DEFAULT=y
CONFIG_DEBUG_BUGVERBOSE=y

# Debug Filesystem (accessed via /sys/kernel/debug)
# Useful for checking GPIO, Clock, and Regulators at runtime.
CONFIG_DEBUG_FS=y

# Magic SysRq Key
# Allows you to send low-level commands (reboot, sync disks, dump task list)
# directly to the kernel, even if the UI freezes.
CONFIG_MAGIC_SYSRQ=y

# Early Printk
# output log messages very early in the boot process (before serial drivers load).
# Essential for diagnosing "Black Screen" boot failures.
CONFIG_EARLY_PRINTK=y

# Memory Debugging (Optional - adds overhead)
# Uncomment if you suspect memory leaks or corruption.
# CONFIG_KASAN=y
